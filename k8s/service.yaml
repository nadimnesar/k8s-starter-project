apiVersion: v1
kind: Service
metadata:
  name: k8s-starter
  namespace: k8s-starter
  labels:
    app: k8s-starter
spec:
  type: NodePort
  selector:
    app: k8s-starter
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
